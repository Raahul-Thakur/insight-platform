# Saved SQL Queries

Put one-off Supabase SQL here.

Suggested naming:

```text
YYYYMMDD_description.sql
```

Examples:

- `20260707_check_startup_counts.sql`
- `20260707_failed_enrichment_jobs.sql`

These files are for manual SQL Editor use. Migration files that change schema should go in `../migrations/`.
